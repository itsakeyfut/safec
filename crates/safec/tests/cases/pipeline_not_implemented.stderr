error: the compilation pipeline is not implemented yet
  = note: safec currently reads its inputs, builds a Safety IR, and writes LLVM IR
  = note: `--emit tokens`, `--emit ast`, `--emit safety-ir` and `--emit llvm-ir` are what it can make
