error: the compilation pipeline is not implemented yet
  = note: safec currently reads its inputs and builds a Safety IR from them
  = note: `--emit tokens`, `--emit ast` and `--emit safety-ir` are what it can make
