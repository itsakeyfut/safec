# Architecture Decision Records

A design decision's rationale lives here and nowhere else. The documents in
[`docs/`](../README.md) say what the project wants to build; the doc comments in
the code say what each type is and how to use it. Neither repeats the reasoning:
they link here.

These records are written in **English**, like the code they constrain, because
they are read by contributors and tooling alike.

Format: [MADR 4.0](https://adr.github.io/madr/), the de facto Markdown ADR
standard. Copy [`adr-template.md`](./adr-template.md) to start one.

## Index

| # | Decision | Status | Confirmed by |
|---|---|---|---|
| [0001](./0001-promote-unproven-results-in-the-sink.md) | Tag a result the analysis could not prove at the check, and promote it in the sink | accepted | unit tests in `crates/safec/src/diagnostics.rs` (promotion on, promotion off, a proven warning left alone, `error_count` equals a recount) the `--safety strict` resolution test in `crates/safec/src/cli.rs`, and `the_sink_is_built_from_the_resolved_options` in `crates/safec/src/driver.rs`; each verified by the mutation that makes it fail |
| [0002](./0002-severity-runs-from-least-to-most-severe.md) | Declare `Severity` from least to most severe, the same direction as `SafetyLevel` | accepted | `severities_are_ordered_from_least_to_most_serious` and `the_worst_severity_in_a_set_is_its_maximum` in `crates/safec/src/diagnostics.rs`; both fail if the variants are put back in descending order |
| [0003](./0003-pass-the-source-map-to-each-render-call.md) | Pass the source map to each render call rather than holding it | accepted | `a_renderer_does_not_hold_the_source_map` in `crates/safec/src/diagnostics/render.rs`; the borrow half does not compile against a renderer that holds the map, and sizing the cache once makes the assertion fail |
| [0004](./0004-resolve-the-strictest-level-where-the-policy-is-built.md) | Resolve the strictest safety level where the policy is built, not only where the options are | accepted | `Policy`'s private field makes the bypass a compile error (E0451); `the_strictest_safety_level_denies_unknown_however_the_policy_is_built` and `the_strictest_safety_level_denies_unknown_even_unresolved` in `crates/safec/src/diagnostics.rs` and `the_strictest_safety_level_denies_unknown_in_the_sink` in `crates/safec/src/driver.rs`, all three of which fail if `Policy::new` keeps `deny_unknown` alone |
| [0005](./0005-hold-each-source-file-behind-an-arc.md) | Hold each source file behind an `Arc` so one can be read while another is added | accepted | `a_file_can_be_read_while_another_is_added` in `crates/safec-ir/src/source.rs`; writing `file` where it writes `file_owned` is the reversal and fails to compile with E0502 |
| [0006](./0006-carry-a-position-rather-than-the-text.md) | Carry a position rather than the text it covers | accepted | `const _: () = assert!(mem::size_of::<Token>() == 16);` in `crates/safec/src/token.rs`; a `String` field takes it to 40 and a `Symbol` field to 20, and the crate stops building. The trigger for an interner is a convention this record states and nothing enforces |

| [0007](./0007-list-the-test-cases-rather-than-discovering-them.md) | List the test cases in a literal table rather than discovering them on disk | accepted | `every_file_in_the_corpus_belongs_to_a_case_in_the_table` in `crates/safec/tests/cases.rs`; putting anything in `cases/` that the table does not name, at the top level or in a subdirectory, makes it fail and nothing else |

| [0008](./0008-hold-the-tree-in-flat-vectors-and-address-a-node-by-index.md) | Hold the tree in flat vectors and address a node by index | accepted | `an_id_still_names_its_node_after_more_are_pushed` in `crates/safec/src/ast.rs`; taking the id from `len()` after the push rather than before makes it fail, and `Ast`'s private vectors make holding a node across a push `error[E0502]` |

| [0009](./0009-name-a-diagnostic-code-after-the-compiler-and-the-topic.md) | Name a diagnostic code after this compiler and the topic, not after the severity | accepted | `Code::new` in `crates/safec/src/diagnostics.rs` asserts the spelling, so `Code::new("E0301")` is `error[E0080]` at the declaration and `a_code_is_sc_and_four_digits_or_it_is_not_a_code` fails if either half of the assertion goes. Which code goes where is held by `each_lexical_diagnostic_keeps_the_code_it_was_assigned`, `each_syntax_diagnostic_keeps_the_code_it_was_assigned` and thirteen `.stderr` files. Which range a topic takes is enforced by nobody but a reviewer, and the record says so |

| [0010](./0010-give-the-graph-an-edge-no-statement-produced.md) | Give the control-flow graph an edge no statement produced | accepted | `E0004`: `Terminator::successors` in `crates/safec-ir/src/ir.rs` matches every kind written out, so a kind added later stops every walk from compiling, and `an_edge_no_statement_produced_can_be_built` stops compiling if the variant goes |

| [0011](./0011-the-ir-crate-depends-on-nothing-in-the-workspace.md) | The IR crate depends on nothing in the workspace, and the frontend depends on it | accepted | cargo, before anything compiles: adding `safec = { path = "../safec" }` to `crates/safec-ir/Cargo.toml` is `error: cyclic package dependency`, and a `use safec::` without it does not resolve either, as `E0432` or `E0433` depending on how far the path gets. The hand-built interpreter tests in `crates/safec-ir/src/interp.rs` are the other half: `a_function_built_by_hand_runs` and five others run the IR with no frontend in the graph |

| [0012](./0012-say-a-local-s-storage-began-and-ended-in-the-block.md) | Say a local's storage began and ended as elements of a block, not as a tree beside it | accepted | `E0004`: a fourth `Element` kind stops `ir.rs`, `interp.rs` and `print.rs` compiling at once, and a field added to one is `E0027` in each walk and `E0063` where the lowering builds it. `the_same_program_in_a_nested_scope_is_not_the_same_ir` and `a_read_through_a_pointer_to_dead_storage_stops_the_run` are the same claim by assertion and by running |

| [0013](./0013-the-translation-unit-carries-the-target-and-answers-what-a-type-is-worth.md) | The translation unit carries the target, and the IR answers what a type is worth | accepted | `TranslationUnit::new` takes a target and has no `Default`, and `Target`'s private fields make the only targets the measured ones. `every_target_is_what_clang_says_it_is` writes the measurements out rather than walking the table, and `what_a_char_holds_follows_the_target` is the difference between two machines: `c = 200` answers -56 and 200 |
| [0014](./0014-write-llvm-ir-as-text-from-a-crate-of-its-own.md) | Write LLVM IR as text, from a crate of its own | accepted | adding `safec = { path = "../safec" }` to `crates/safec-llvm/Cargo.toml` is `error: cyclic package dependency`, and a `use safec::` without it does not resolve either. Four corpus cases hold the text and `the_emitted_ir_is_what_llvm_accepts` in `crates/safec/tests/llvm.rs` holds that the text is LLVM: spelling `Add` as `Sub` changes every corpus expectation and leaves that test passing. `a_unit_built_by_hand_becomes_a_module` in `crates/safec-llvm/src/emit.rs` writes a whole module with no frontend in the graph |
| [0015](./0015-make-an-object-by-spawning-clang.md) | Make an object by spawning `clang`, not by linking LLVM | accepted | `an_object_is_for_the_machine_the_run_named` in `crates/safec/tests/object.rs` makes an object for all eight targets and reads what each one is out of its own header, against eight measurements written out rather than walked; passing the host triple to `clang` rather than the one the run named fails seven rows here and a different seven on each CI runner. `an_object_the_linker_accepts` links the host object and runs it, and `a_missing_clang_says_what_to_install` runs the compiler with an emptied `PATH` |
| [0016](./0016-an-analysis-is-a-trait-and-an-unreached-block-has-no-value.md) | An analysis is a trait, and a block nothing has reached has no value | accepted | `the_back_edge_changes_the_answer_after_the_loop` in `crates/safec-ir/tests/written.rs`: cutting the solver to one visit per block fails it and fails nothing else, and so does a solver that answers that nothing moved. An analysis whose `join` returns a `bool` is `error[E0053]`. `a_block_nothing_reaches_has_no_answer` fails if an unreachable block is given `on_entry` instead of nothing, and `what_a_loop_writes_and_what_comes_before_it_are_answered_apart` fails if the first arrival at a block is joined into `on_entry` rather than stored. A fifth method on `Analysis` without a default is `error[E0046]` at every implementation |

| [0017](./0017-record-each-half-of-an-escape-where-its-subject-lives.md) | Record each half of an escape where its subject lives: the local's staleness on the local, the heap's doubt on the site | accepted | `error[E0596]` at both call sites if `Known::reached_by` is spelled `&mut self`, because reporting takes `&Known` and the transfers take `&mut Known`. Seven mutations in `crates/safec-ir/src/memory.rs`, each failing named tests and nothing else: dropping the `Reached::Lost` from `reached_by` fails `a_pointer_replaced_through_its_alias_after_a_free` and `a_pointer_replaced_through_its_own_address`, and dropping `unproved` at the `Rvalue::Address` arm, after the `Element::Assign` match, at the call's destination, or `settle` from `join`, fails `an_allocation_shared_with_a_local_whose_address_escaped`, `an_allocation_given_to_an_escaped_local_after_the_escape`, `a_call_into_a_local_whose_address_escaped` and `an_escape_on_one_arm_and_a_shared_allocation_on_the_other` respectively. Answering `Reached::Lost` for an escaped local with no sites fails `an_escaped_local_that_reaches_no_site_at_all`, and answering an escaped local's sites as `Lost` rather than listing them fails `a_free_through_an_escaped_local_is_seen_by_a_sharer` |

| [0018](./0018-a-site-names-one-allocation-at-a-time.md) | A site names one allocation at a time, and the locals that held the last one say so | accepted | `error[E0063]` in `Held::none` and `error[E0027]` in `Held::clear` and `Held::union` if a field is added to `Held`, which is what holds its two halves together. Seven mutations in `crates/safec-ir/src/memory.rs`, each failing named tests and nothing else: the reset not calling `Held::lose`, `lose` keeping the bit it reports gone, `Held::clear` leaving `lost` set, `reached_by` not reading it, `Held::union` not unioning it, and the rejected option in its place, which fail `a_pointer_saved_across_a_loop_that_allocates_again`, `a_copy_of_a_local_that_lost_its_allocation_lost_it_too`, `a_double_free_across_a_loop_names_the_free_that_is_wrong`, `a_free_of_the_previous_turns_pointer_leaves_the_new_one_proved`, `a_local_given_something_fresh_forgets_what_it_lost` and `a_loop_that_allocates_and_frees_each_turn_is_proved` between them, and reading `lost` only for a local that reaches no site fails `a_local_that_kept_one_allocation_and_lost_another`. A field added to `Known` is `error[E0027]` at `Known::reborn`, which is where a site stops naming what it named. `Analysis::height` is the one claim nothing holds and the record says so |

| [0019](./0019-follow-a-write-through-a-pointer-only-where-it-lands.md) | Follow a write through a pointer where this check knows where it lands, and do nothing where it does not | accepted | `error[E0063]` and `error[E0027]` if a field is added to `Held`. Mutations in `crates/safec-ir/src/memory.rs`, each failing named tests and nothing else: the `Deref` arm never firing and `Rvalue::Address` not recording the edge each fail `a_free_through_a_pointer_that_reached_another_allocation` and four beside it; replacing rather than unioning fails `a_write_through_a_pointer_that_may_land_elsewhere_keeps_what_was_there`; unproving the target fails `a_write_through_an_alias_leaves_a_sharer_s_proof_alone` and four beside it; carrying every allocation where it can follow none fails `a_write_through_an_alias_that_carries_no_allocation`; letting the edge survive pointer arithmetic fails `a_write_through_an_address_plus_one_is_not_a_write_to_the_local`; and dropping the edge from `Held::union` fails `an_address_taken_on_one_arm_is_written_through_after_the_join`. A variant added to `Rvalue` is `error[E0004]` here as at every other reader. `Analysis::height` is held by nothing and the record says so |

| [0020](./0020-a-free-of-a-may-set-is-a-fact-about-the-set.md) | A free of a may-set is a fact about the set, and it replaces what the members say | accepted | `error[E0004]` at `verdict` if a variant is added to `Reached`, and `error[E0063]`/`error[E0027]` if a field is added to `Held`. Seven mutations in `crates/safec-ir/src/memory.rs`, each failing named tests and nothing else: marking every member `Freed` again fails `a_free_of_one_of_two_allocations_by_name`; recording nothing on the local, and answering the set fact beside the members rather than instead of them, each fail `a_branch_that_allocates_either_way` and `a_free_of_either_of_two_locals_names_no_allocation`; unioning the proof rather than intersecting it fails `a_free_of_a_may_set_on_one_arm_only`; keeping the fact through `Held::clear` fails `a_local_given_nothing_forgets_the_set_it_freed`; and firing on a single site fails every proved double free and use after free this check makes, in the corpus and in `freed.rs` alike. `Analysis::height` is held by nothing and the record says so |

| [0021](./0021-fold-a-zero-pointer-offset-where-the-ir-is-built.md) | Fold a zero pointer offset where the IR is built, so no analysis sees two shapes for one expression | accepted | Five mutations in `crates/safec/src/lowering.rs`, each failing named tests and nothing else: `unmoved` answering `None` fails the three cases that hold the four spellings agreeing and the three older subscript cases on their IR; dropping its question about the type fails `a_zero_added_to_an_integer_keeps_its_operation`, which guards the IR's shape and not a computed value; asking about the subscript's type rather than the base's fails the three older subscript cases; and reading the zero on one side of `Add` only, or not answering for `Sub`, fails one of the new cases each. Every diagnostic in the corpus is byte-identical to what the rejected option produced |

| [0022](./0022-say-where-c-sequences-one-evaluation-before-another.md) | Say where C sequences one evaluation before another, as an element of a block | accepted | `error[E0004]` at every reader of `Element` if the kind goes and at `verdict` if a variant is added to `Kind`, and `error[E0063]` at `Freeing::new` and `Freeing::joined` if a field is added to the pair a free carries. Mutations in `crates/safec/src/lowering.rs` and `crates/safec-ir/src/memory.rs`, each failing named tests and nothing else. Not emitting the element, and not reading it, each fail every proved double free and use after free in the corpus and in `freed.rs`; dropping the marker for `,`, `&&`, `||` or `?:` fails the case named for that operator. The rows that matter are the ones whose mutation makes the compiler certain rather than quiet: a marker that stops asking what encloses it, for the comma, for a call's arguments, for `&&` and `||` and for `?:`, fails `an_unsequenced_free_and_use_is_not_proved` and the three `..._orders_nothing` cases; `Freeing::new` starting sequenced fails seven; and `Freeing::joined` taking the stronger flag or its own span fails `a_free_sequenced_on_one_arm_only_is_not_a_proof`. Three more hold where the order was the wrong question: a double free asking for a sequence point, a later free replacing one already sequenced, and the fold keeping a caret where several frees met. `Analysis::height` is held by nothing and the record says so |

| [0023](./0023-carry-a-read-forwards-to-the-free-it-is-unordered-against.md) | Carry a read forwards to the free it is unordered against, as a field of the memory lattice | accepted | `error[E0027]` at `Allocations::join` and `Known::reborn`, which name every field of `Known`, so a field added to that value answers for what a merge and a rebirth do to it. Eight mutations in `crates/safec-ir/src/memory.rs`, each failing named tests and nothing else: the element transfer recording nothing fails `a_write_through_a_pointer_the_check_meets_first_is_reported` and `a_discarded_read_the_check_meets_first_is_reported`; the terminator transfer recording nothing, and concluding `Unsafe` rather than `Unknown`, each fail `an_unsequenced_use_the_check_meets_first_is_reported` and two beside it; not comparing the sites fails `a_use_of_another_pointer_before_a_free_is_not_reported`; not folding `made` with `same` fails `a_read_of_either_of_two_allocations_before_a_free_names_neither`; leaving `pending` alone in `Known::reborn` fails `a_read_of_a_site_handed_to_a_second_allocation_is_not_carried_to_its_free`; and dropping the place from the key fails `a_proof_replaces_the_suspicion_at_one_caret`. Not clearing at a sequence point fails `a_comma_that_frees_after_it_reads`, a program C17 6.5.17 p2 defines. For the marker: an arm not beginning with it fails `a_condition_read_through_a_pointer_is_sequenced_before_the_body` and `..._before_the_loop_exits`, only the taken arm beginning with it fails `..._before_the_other_arm` on its `.stderr`, and demanding one where a `for` has no condition fails `a_for_loop_asks_before_each_turn_and_steps_after_each_body`. The record names the three things held by nothing: the two arms of one conditional, the end of a span in the key, and recording before the transfer's arms. `Analysis::height` is held by nothing and the record says so |

| [0024](./0024-a-proof-survives-while-the-set-it-is-about-does-not-grow.md) | A proof survives while the set it is about does not grow, and the lattice's join is a second method | accepted | `error[E0027]` at `Held::joined` and `Held::accumulated`, which name every field, so a fact added to `Held` answers for what it means in each role. Four mutations in `crates/safec-ir/src/memory.rs`, each failing named tests and nothing else: not restoring the proof for a sole operand fails `a_free_after_an_offset_that_kept_the_set_is_proved` and the two `..._keeps_the_proof` cases ADR-0030 put beside it; restoring it whenever any operand carries one fails `an_offset_by_a_second_pointer_loses_the_proof` in `crates/safec-ir/tests/freed.rs`, which is the fabricated-proof direction a review measured and which is hand-built because ADR-0030 leaves no C this frontend accepts able to reach it; and `joined` accumulating the proof fails `a_free_of_a_may_set_on_one_arm_only`. Dropping the proof at a write through a pointer is held by nothing, because ADR-0017's escape answers `Unknown` there whatever the field says, and the record says so |
| [0025](./0025-a-dereference-that-was-reported-refines-the-pointer.md) | A dereference refines the pointer it went through to non-null, on the path that reached it | accepted | `a_pointer_dereferenced_twice_is_reported_once` in `crates/safec/tests/cases`, which reports both dereferences and fails if the call to `met` is dropped from `crate::nullability`'s `Analysis::element`; and `a_pointer_dereferenced_on_one_arm_is_not_proved_after_it`, which is the half that says the fact is per path rather than per function and fails under the mutation in `Nullness::joined` that answers `NonNull` whenever either side holds it, so that a merge keeps the refinement instead of taking it away. `error[E0004]` at `Nullness::inverted` and `Nullness::concluded` if a fourth state is added |

| [0026](./0026-say-that-a-call-s-arguments-have-been-evaluated.md) | Say that a call's arguments have been evaluated, as an element of its own | accepted | `error[E0004]` at nine matches over `Element` in three crates if the kind goes, and `error[E0027]` at the six that walk the IR if a field is added to it; the other three ask only which kind it is and the record names them. Three mutations in `crates/safec/src/lowering.rs` and `crates/safec-ir/src/memory.rs`, each failing named cases and nothing else: the arm that reads the element doing nothing fails `a_read_in_a_frees_own_argument_is_ordered_before_it` and `a_read_in_a_frees_argument_through_a_call_is_ordered_before_it` on their `.stderr`; emitting it whatever encloses the call deletes the `SC0402` from `an_unsequenced_use_the_check_meets_first_is_reported` and three cases beside it, which is the silent direction; and giving it `Element::Sequenced`'s whole meaning turns `a_comma_inside_a_call_argument_orders_nothing_outside_it` from a warning into an error about an order C has not chosen. `a_read_in_an_argument_with_no_marker_is_reported_unproven` in `crates/safec-ir/tests/freed.rs` holds the boundary an adapter that never emits the element lands on, and fails if the element is inserted. That the element is emitted for a call rather than for something a call happens to be is held by nothing, and the record says so |

| [0027](./0027-an-unsafe-conclusion-is-about-an-execution-this-function-has.md) | An `Unsafe` conclusion is about an execution this function has, and only a pointer established null exempts a free | accepted | `a_double_free_in_a_function_the_only_caller_passes_null_to_is_proved` in `crates/safec/tests/cases`, whose `main` passes `0` to a function that frees its parameter twice; requiring `made.is_some()` in `verdict`'s `Unsafe` arm in `crates/safec-ir/src/memory.rs`, which is the option the record rejects, fails it along with `a_parameter_freed_twice` and every other case whose proof stands on a site this check did not watch being allocated. No mutation fails it alone and the record says so |

| [0028](./0028-replace-what-a-target-held-where-a-write-must-land-in-it.md) | Replace what a target held where a write through a pointer must land in it, and say when the edge is all of it | accepted | `error[E0063]` in `Held::none` and `error[E0027]` in `Held::clear`, `Held::joined` and `Held::accumulated` if a field is added to `Held`. Six mutations in `crates/safec-ir/src/memory.rs`, each failing named tests and nothing else: unioning always, and `Rvalue::Address` not clearing the flag on the destination, each fail `a_free_through_a_pointer_that_reached_another_allocation` and the six cases beside it; replacing wherever the set names one local, and `Held::joined` intersecting the flag rather than unioning it, each fail `a_write_through_a_pointer_with_one_target_on_one_arm_only` and `an_address_taken_on_one_arm_is_written_through_after_the_join` with an `error[SC0402]` about a program that frees each allocation once; the condition not reading `Known::escaped` fails `a_write_through_a_pointer_whose_own_address_escaped` and `a_write_through_a_pointer_whose_address_escaped_before_it_was_given_its_target`, which are the two orders the escape can be written in; and `Rvalue::Address` clearing the flag whatever place is taken fails `the_address_of_a_dereference_is_not_an_edge_to_the_local` in `crates/safec-ir/tests/freed.rs`, which is hand-built IR because no C program reaches the shape. Five of the flag's answers are held by nothing, for reasons the record gives, and `Analysis::height` is held by nothing either |
| [0029](./0029-a-call-this-check-cannot-read-replaces-what-an-escaped-local-holds.md) | A call this check cannot read replaces what an escaped local holds, and a free of it proves nothing | accepted | Mutations in `crates/safec-ir/src/memory.rs`, each failing named tests: dropping the `Known::replaced` call from the `Callee::Opaque` arm, and `Callee::Frees` writing `Freed` although the argument's local is `lost`, each fail `a_call_this_check_cannot_read_may_have_replaced_what_an_escaped_local_holds`, `a_certain_write_does_not_survive_a_call_this_check_cannot_read`, `a_double_free_through_a_sharer_after_an_opaque_call_is_not_proved` and `a_double_free_a_call_took_the_proof_of_still_fails_a_build_that_denies_unknown`; `Callee::Frees` keying on the `Reached::Lost` the fold already produces rather than on the local's own row fails `a_free_through_an_escaped_local_is_seen_by_a_sharer`; `Known::replaced` not reading `Known::escaped` fails `an_opaque_call_before_the_escape_leaves_the_proof_alone`; marking a local that holds no site fails `a_write_through_a_pointer_whose_own_address_escaped` and `a_write_through_a_pointer_whose_address_escaped_before_it_was_given_its_target`; writing over a site an earlier free already proved fails `a_free_this_check_could_not_follow_leaves_a_proved_free_alone`; and clearing the escaped local's row instead of marking it fails `a_use_after_free_through_an_escaped_local_is_still_reported_after_an_opaque_call` by saying nothing about the read. That `free` and `malloc` do not mark, and the two arms of `Allocations::holds_something_unnameable` that skip an argument, are held by nothing, which the record says and measures |
| [0030](./0030-a-pointer-operand-decides-what-pointer-arithmetic-reaches.md) | A pointer operand decides what pointer arithmetic reaches, and where there is none every operand still does | accepted | `error[E0061]` at both callers if the predicate is dropped from `built_from`'s signature, so a third caller cannot be written without answering this, and `error[E0004]` at every reader of `Ty` if a fourth kind is added. Five mutations in `crates/safec-ir/src/memory.rs`, each failing named tests and nothing else: never consulting the predicate fails `a_subscript_of_a_freed_pointer`, `an_offset_by_an_integer_local_keeps_the_proof` and `an_offset_by_an_integer_parameter_keeps_the_proof`, each back to a warning; keeping the operands that are not pointers fails `a_subscript_of_a_freed_pointer` and `an_index_written_on_the_left_still_carries_the_pointer`, whose `SC0402` goes silent, and every other case whose arithmetic has a pointer in it; taking the left operand fails `an_index_written_on_the_left_still_carries_the_pointer`, `an_index_that_is_a_freed_pointer_is_still_reached` and `an_offset_by_a_second_pointer_loses_the_proof`; dropping the fallback fails `an_addition_of_two_integers_carries_what_both_hold`, which is the row-6 direction; and letting two pointer operands keep the proof fails `an_offset_by_a_second_pointer_loses_the_proof`. The last four are hand-built in `crates/safec-ir/tests/freed.rs`, because this frontend refuses each of their shapes with `SC0304`. A fifth, `an_allocation_in_a_local_declared_int_is_dropped_beside_a_pointer`, holds the boundary `docs/c-family.md` now names: a local that may hold an allocation has to be declared as a pointer, and what breaking it costs is a silence |
| [0031](./0031-a-write-this-check-cannot-pin-down-replaces-what-an-escaped-local-holds.md) | A write this check cannot pin down replaces what an escaped local of the written type holds | accepted | Nine mutations in `crates/safec-ir/src/memory.rs`, each failing named tests and nothing else. Dropping the `replaced_by` call fails seven at once: `a_write_through_a_pointer_this_check_cannot_follow_may_have_replaced_what_an_escaped_local_holds`, `a_write_that_replaces_what_an_escaped_local_holds_needs_no_call_and_no_parameter`, `a_write_through_more_than_one_deref_may_have_replaced_what_an_escaped_local_holds`, `a_write_through_a_character_pointer_may_have_replaced_what_any_escaped_local_holds`, `a_write_that_may_land_beside_its_target_may_have_replaced_what_an_escaped_local_holds`, `a_write_whose_type_this_check_cannot_name_distrusts_every_escaped_local` in `crates/safec-ir/tests/freed.rs`, and `a_use_after_free_a_write_took_the_proof_of_still_fails_a_build_that_denies_unknown`, which is the first of them with the flag on. The predicate answering `true` for every local, and comparing the types with `!=`, each fail `a_write_through_a_pointer_to_an_int_leaves_what_an_escaped_local_holds_alone` and `a_write_this_check_is_certain_about_leaves_what_another_escaped_local_holds_alone`. Four more fail one case each: keying the door on the shape this check can follow, requiring an empty target set as well, ignoring ADR-0028's certainty condition, and dropping the character type from the exception. Spelling the fallback `is_some_and` rather than `is_none_or` fails the hand-built case alone. `error[E0061]` at the `Callee::Opaque` call site if the predicate is dropped from `Known::replaced`. That a callee's predicate is unnarrowed is held by nothing and the record says so |

| [0032](./0032-bound-what-is-unchecked-inside-a-declared-hatch.md) | Bound what is unchecked inside a declared hatch, and check its boundary rather than its body | proposed | Nothing. There is no hatch in the tree, no annotation, and no check that consults one, and the record says so. The guard to build with the first hatch is a test that a hatch changes what a conclusion is about and not what it concluded |
| [0033](./0033-a-conclusion-this-analysis-could-not-prove-does-not-build.md) | A conclusion this analysis could not prove does not build, once a program has asked to be checked | proposed | Nothing guards the new rule, and `a_lower_safety_level_leaves_deny_unknown_to_its_flag` in `crates/safec/src/cli.rs` holds the old one. Landing this rewrites that test rather than adding beside it; the mutation that must then fail is resolving the default the way it resolves now |
| [0034](./0034-a-diagnostic-that-rejects-a-program-carries-the-change-that-would-make-it-compile.md) | A diagnostic that rejects a program carries the change that would make it compile | accepted | The build, not a test. Removing the `Remedy` parameter from `Diagnostic::concluded` and the two `with_remedy` calls in its arms is `error[E0061]` at `memory_finding` and `nullability_finding` in `crates/safec/src/driver.rs`, which are every place a safety finding is built, and at the call sites in the test modules under `cargo test`. Reaching the reader takes two more: `a_remedy_is_written_after_the_notes_on_both_rendering_paths` in `crates/safec/src/diagnostics/render.rs` fails when the `write_remedies` call is dropped from `write_one`, and again when it is dropped from `render_header_only`. That `Diagnostic::error` is public, so a rejection built through it carries no remedy, is held by nobody and the record says so |

**By status**: accepted: 0001, 0002, 0003, 0004, 0005, 0006, 0007, 0008, 0009, 0010, 0011, 0012, 0013, 0014, 0015, 0016, 0017, 0018, 0019, 0020, 0021, 0022, 0023, 0024, 0025, 0026, 0027, 0028, 0029, 0030, 0031, 0034 · proposed: 0032, 0033 · superseded: none

Records are numbered consecutively from `0001`.

## Where each kind of writing belongs

| Location | Holds | Does not hold |
|---|---|---|
| [`docs/*.md`](../README.md) | what the project wants to build, roughly: the concept, the safety model, the architecture, the phases | how a decision was reached. These are intent documents, not specifications, and a design may knowingly diverge from one. Say so in the record and update the document |
| doc comments in the code | what a type is, how to use it, and the local reason it is shaped that way | cross-cutting rationale; link here instead |
| `docs/adr/**` | why a cross-cutting decision was chosen, when, and what would reverse it | type or signature detail; link to the code |

## When to write one

A record is a cache of reasoning that the code cannot show. The question that
decides whether to open one is whether someone will later undo this in good
faith: reading the code, seeing something that looks better, and having no way
to find out what it costs.

* Two or more implementations are possible and one is chosen, especially when the
  choice shapes an interface that later phases will be written against.
* A choice affects whether the compiler can be *trusted*: anything that decides
  when a result is reported, suppressed, or promoted. Getting one of these wrong
  makes `safec` claim code is safe that was never proven, which is the worst
  failure this project has.
* The code was knowingly left in a shape that looks wrong: a duplication kept on
  purpose, a slower path chosen for a reason, a guard that appears redundant.
  Without a record these are cleaned up by the next person to read them.
* An existing decision is reversed: write a new record, mark the old one
  `superseded by ADR-NNNN`, and note what changed.
* A design knowingly diverges from a document in `docs/`: record why, and update
  that document in the same change.
* You are about to write "undecided" into a doc comment: open one as `proposed`.

## When not to write one

A reason has cheaper places to live, and each of these is a complete answer on
its own:

1. Nothing. The decision is local and the code already shows it.
2. A comment where it happens.
3. A doc comment on the type or function it constrains.
4. A test whose name states the rule.
5. A record here.

Take the lowest one that holds. This repository leans hard on 4: a test named
for the rule it protects is how most decisions here are written down, and the
mutation that makes it fail is the proof it is a real rule. Open a record only
when the reasoning does not fit into a name and an assertion, which usually
means the rejected alternatives are the part worth keeping.

Specifically, do not open one when:

* It affects one call site, or it is formatting.
* Reversing it would be cheap and local. The record earns its keep by making a
  reversal informed, so a reversal that costs an afternoon does not need one.
* A test name already says it, and someone who broke the rule would read that
  name and understand what they broke.
* The choice was made for us, by the C standard, by a dependency's interface, or
  by the platform. Writing that down records a fact rather than a decision.
* It is a performance choice a benchmark can settle. The benchmark is the
  record, and it stays true when the numbers change.

Naming is usually below the line too, but it rises above it when the name shapes
an API that the rest of the crate compares against or matches on, because those
are the names that go silently wrong.

**Expected rate.** Phase 0 is dense because it is almost entirely interface, and
that is the phase where records are worth the most. It does not continue. Most
of what a lexer and a parser decide is decided by the C standard, and most of
what an analysis decides is settled by measurement. A few records per phase is
the shape to expect. Weeks with none are the normal case, and several in a week
is a sign the bar has slipped rather than a sign of progress.

## Conventions

* Filename `NNNN-short-slug.md`, numbers consecutive.
* The four digits are there so that sorting the filenames as text puts them in
  the order they were written. They are not a limit. A number is never reused,
  never renumbered, and stays with a record that is superseded, because it is
  what every link, doc comment and commit message refers to. Widening the
  padding later would break all of those at once, so a log that ever reaches
  `9999` carries on into five digits and fixes its ordering in the index above.
  A log that large is a sign the bar in *When not to write one* was too low.
* MADR statuses: `proposed`, `accepted`, `rejected`, `deprecated`,
  `superseded by ADR-NNNN`.
* Every record fills in **Confirmation**: which test or guard fails if the
  decision is violated, **and the change to the code that makes it fail**. This
  repository verifies guards by breaking the code and watching the suite go red,
  so a record should name that mutation. If nothing would fail, say so.
* A `proposed` status while the codebase already relies on the decision is itself
  a defect; say so in *Context and Problem Statement*.
* Keep the status in sync between an ADR's front matter and its row in this index.
* When a record says something is not yet enforced, and it then is, update
  **Confirmation** in the same change. That section answers what guards the
  decision now, not what was true when it was written, and a record that
  understates its own guards sends the next reader to redo finished work. It is
  the same obligation as the pointers below, in the section that matters most.
* When code a record names moves or is renamed, update the record's pointers to
  it. The reasoning is a record of what was thought at the time and is not
  rewritten, but the pointers are navigation rather than history, and a record
  whose pointers are dead is a record nobody can check.
* Prose here uses no em dashes.

## More Information

* [MADR 4.0](https://adr.github.io/madr/): the template this follows.
* [`adr-template.md`](./adr-template.md): copy this to start a new record.
